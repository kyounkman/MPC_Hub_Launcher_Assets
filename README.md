# MPC Hub Launcher Assets

Public image-only hosting for the MPC Hub Google Workspace add-on.

This repository contains only the approved launcher logo and four tool icons. The Apps Script source, dashboard URLs, configuration, and user data remain in the private application repository.
